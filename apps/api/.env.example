PORT=3000

# postgresql
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=restaurant_db

# keycloak admin console
KC_ADMIN_USERNAME=admin
KC_ADMIN_PASSWORD=admin

# keycloak api config (usada por dotenv al correr npm run dev localmente)
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/restaurant_db
KEYCLOAK_BASE_URL=http://localhost:8080
KEYCLOAK_REALM=restaurant-realm
KEYCLOAK_CLIENT_ID=restaurant-api
KEYCLOAK_CLIENT_SECRET=<obtener-de-keycloak-admin-console>
KEYCLOAK_ADMIN_CLIENT_ID=admin-cli
KEYCLOAK_ADMIN_CLIENT_SECRET=<obtener-de-keycloak-admin-console>

# DB_ENGINE=postgres   (opcional — por defecto es "postgres"; cambiar a "mongo" en Fase 4)

# mongodb (requerido cuando DB_ENGINE=mongo)
# MONGODB_URI=mongodb://localhost:27017/restaurant_db
# MONGO_REPLICA_SET=rs0   (solo necesario si se conecta a un replica set)

# redis — cache de respuestas de lectura frecuente (Fase 6)
# apunta a localhost cuando la API corre fuera de Docker; a redis:// cuando esta en compose
REDIS_URL=redis://localhost:6379
